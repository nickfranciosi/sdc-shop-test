          {% if product.has_only_default_variant %}
            <td class="{% if product.id == current_product.id %}product-table-item-is-current-product{% endif %}" data-label="{{product.title}}">
              <div class="inner">
                {{product.metafields.comparison.eight_day_treatments}}
              </div>
            </td>
          {% else %}
            {% for variant in product.variants %}
              <td data-label="{{product.title}} - {{variant.title}}">
                <div class="inner">
                  {{variant.metafields.comparison.eight_day_treatments}}
                </div>
              </td>
            {%  endfor %}
          {% endif %}